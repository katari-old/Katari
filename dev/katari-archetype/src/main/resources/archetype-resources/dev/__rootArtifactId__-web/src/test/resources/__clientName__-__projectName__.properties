#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )

${symbol_pound} dataSource.jdbcUrl=jdbc:mysql://localhost/database
${symbol_pound} dataSource.user=username
${symbol_pound} dataSource.password=password

dataSource.jdbcUrl=${symbol_dollar}{${clientName}_${projectName}.datasource.url}
dataSource.user=${symbol_dollar}{${clientName}_${projectName}.datasource.username}
dataSource.password=${symbol_dollar}{${clientName}_${projectName}.datasource.password}

${symbol_pound} Enables the runtime validation of html. This is usually very slow and not
${symbol_pound} adequate for production.
htmlValidator.enabled=true

${symbol_pound} Enables debug mode. The meaning of this flag is up to each module. For
${symbol_pound} example:
${symbol_pound} - The static content servlet serves content directly from the file system
${symbol_pound} - The freemarker view resolver first searches for templates from the FS.
debugMode.value=true

