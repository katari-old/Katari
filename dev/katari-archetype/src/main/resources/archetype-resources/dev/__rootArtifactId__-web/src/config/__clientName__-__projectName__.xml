#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<!-- Sample tomcat context configuration. Set docBase to the correct location.
saveOnRestart must be false because otherwise the context will not be loaded
untill the session expires (or so it looks like ...)
-->
<Context docBase="/home/gpablo/projects/globant/${clientName}-${projectName}/main/${parentArtifactId}/${clientName}-${projectName}-web/src/main/webapp"
	path="${clientName}-${projectName}-i" reloadable="true" override='true' saveOnRestart='false'>
  <WatchedResource>*</WatchedResource>
</Context>

